// budget-expense-tracker.routes.js
const { Router } = require('express')
const BudgetExpenseTrackerController = require('./budget-expense-tracker.controller')

const router = Router()

// Define routes for budget-expense-tracker
// Example: router.get('/', Budget-expense-trackerController.someMethod)

module.exports = router